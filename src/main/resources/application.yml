spring:
  main:
    banner-mode: off

services:
  repository:
    project: ~
    checks: []

adapters:
  bit-bucket:
    url: ~
    token: ~
  gitea:
    url: ~
    token: ~

security:
  allowed-origins: ~
