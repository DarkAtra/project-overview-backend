spring:
  main:
    banner-mode: off

management:
  endpoints:
    enabled-by-default: false
  endpoint:
    health:
      enabled: true
    info:
      enabled: true

projectoverview:
  repository:
    dir: ./repositories
  security:
    allowed-origins: ~

---
spring:
  profiles: dev

projectoverview:
  security:
    allowed-origins: http://localhost:3000
